package adapter.`in`.http

import domain.contract.NumberQueueWriter
import java.net.Socket

class HandleClientConnection(client: Socket, queueWriter: NumberQueueWriter) {
    fun run() {
        while(true) {

        }
    }
}